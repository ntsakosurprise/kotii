# removeImportsWebpackPlugin

<h2><strong>What it is?</strong></h2>

<p>This is a plugin that helps us remove import statements on source code. Currently, this is done on some custom internal file.</p>

<h2><strong>Why was it created?</Strong></h2>

<p>This plugin was created as result of an issue that </p>

## How It works

It checks if webpack has completed compilation with errors
It then throws an error if webpack compilation has errors.
