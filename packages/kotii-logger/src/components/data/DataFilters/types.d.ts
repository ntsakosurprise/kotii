import { BaseProps } from "../../../types";
export interface PageHeaderProps extends BaseProps {
    gridArea?: string;
    id?: string;
    drop?: boolean;
    heading?: string;
    layer?: boolean;
}
